# 🚀 TaxToken DApp 部署指南

本指南将教你如何将 DApp 部署到互联网上，让全世界的用户都能访问。

---

## 📋 目录

1. [准备工作](#准备工作)
2. [方案一：Vercel 部署（推荐）](#方案一vercel-部署推荐)
3. [方案二：Netlify 部署](#方案二netlify-部署)
4. [方案三：IPFS 去中心化部署](#方案三ipfs-去中心化部署)
5. [自定义域名配置](#自定义域名配置)
6. [常见问题](#常见问题)

---

## 准备工作

### 1. 确保项目可以正常构建

在部署前，先测试本地构建：

```bash
# 进入项目目录
cd tax-token-dapp

# 安装依赖（如果还没安装）
npm install

# 构建生产版本
npm run build

# 本地预览构建结果
npm run preview
```

如果构建成功，你会在 `dist/` 文件夹中看到构建好的文件。

### 2. 准备 GitHub 账号

- 访问 [GitHub.com](https://github.com) 注册账号（如果还没有）
- 创建一个新的仓库来存放你的代码

---

## 方案一：Vercel 部署（推荐）⭐⭐⭐⭐⭐

**优点：**
- 🚀 最简单快速
- 💰 完全免费
- 🔒 自动 HTTPS
- 🌍 全球 CDN
- 🔄 自动部署

### 步骤 1：上传代码到 GitHub

```bash
# 如果还没有远程仓库，先在 GitHub 上创建一个仓库
# 然后执行以下命令：

git remote add origin https://github.com/你的用户名/tax-token-dapp.git
git branch -M main
git push -u origin main
```

### 步骤 2：部署到 Vercel

#### 方法 A：通过 Vercel 网站（推荐）

1. **访问 Vercel**
   - 打开 [vercel.com](https://vercel.com)
   - 点击 "Sign Up" 注册账号（可以用 GitHub 登录）

2. **导入项目**
   - 登录后，点击 "Add New..." → "Project"
   - 选择 "Import Git Repository"
   - 授权 Vercel 访问你的 GitHub
   - 选择 `tax-token-dapp` 仓库

3. **配置项目**
   - **Framework Preset**: 选择 "Vite"
   - **Root Directory**: `.` (默认)
   - **Build Command**: `npm run build` (自动识别)
   - **Output Directory**: `dist` (自动识别)
   - **Install Command**: `npm install` (自动识别)

4. **部署**
   - 点击 "Deploy" 按钮
   - 等待 1-2 分钟
   - 🎉 部署完成！

5. **获取网址**
   - Vercel 会自动分配一个网址，格式类似：
     ```
     https://tax-token-dapp.vercel.app
     ```
   - 你可以立即访问这个网址测试你的 DApp

#### 方法 B：通过 Vercel CLI（命令行）

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 登录 Vercel
vercel login

# 3. 部署项目
cd tax-token-dapp
vercel

# 4. 按照提示操作：
# - Set up and deploy? Yes
# - Which scope? 选择你的账号
# - Link to existing project? No
# - Project name? 回车（使用默认名称）
# - In which directory? ./ (回车)
# - Override settings? No

# 5. 部署到生产环境
vercel --prod
```

### 步骤 3：自动部署设置

**好消息：** 一旦项目连接到 Vercel，每次你推送代码到 GitHub，Vercel 会自动重新部署！

```bash
# 修改代码后
git add .
git commit -m "更新功能"
git push

# Vercel 会自动检测并重新部署
```

---

## 方案二：Netlify 部署

**优点：**
- 🚀 简单易用
- 💰 免费
- 🔒 自动 HTTPS
- 📊 提供分析数据

### 步骤 1：上传代码到 GitHub（同方案一）

### 步骤 2：部署到 Netlify

1. **访问 Netlify**
   - 打开 [netlify.com](https://netlify.com)
   - 用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add new site" → "Import an existing project"
   - 选择 "Deploy with GitHub"
   - 选择 `tax-token-dapp` 仓库

3. **配置构建设置**
   ```
   Build command: npm run build
   Publish directory: dist
   ```

4. **部署**
   - 点击 "Deploy site"
   - 等待几分钟
   - 获得网址：`https://你的项目名.netlify.app`

---

## 方案三：IPFS 去中心化部署

**优点：**
- 🌐 完全去中心化
- 🔒 抗审查
- 💰 免费

**缺点：**
- 访问速度可能较慢
- 需要使用 IPFS 网关

### 步骤 1：构建项目

```bash
npm run build
```

### 步骤 2：上传到 IPFS

#### 方法 A：使用 Fleek（推荐）

1. 访问 [fleek.co](https://fleek.co)
2. 用 GitHub 登录
3. 添加新站点，选择仓库
4. 配置构建设置（同 Vercel）
5. 部署后获得 IPFS 链接

#### 方法 B：使用 Pinata

1. 访问 [pinata.cloud](https://pinata.cloud)
2. 注册账号
3. 上传 `dist` 文件夹
4. 获得 IPFS 哈希
5. 通过网关访问：`https://gateway.pinata.cloud/ipfs/你的哈希`

---

## 自定义域名配置

### 在 Vercel 上配置自定义域名

1. **购买域名**
   - 推荐平台：Namecheap、GoDaddy、阿里云、腾讯云

2. **在 Vercel 添加域名**
   - 进入项目设置 → Domains
   - 输入你的域名（如 `www.mytoken.com`）
   - 点击 "Add"

3. **配置 DNS 记录**

   在你的域名提供商处添加以下记录：

   **方式 A：使用 CNAME（推荐）**
   ```
   类型: CNAME
   名称: www
   值: cname.vercel-dns.com
   ```

   **方式 B：使用 A 记录**
   ```
   类型: A
   名称: @
   值: 76.76.21.21
   ```

4. **等待生效**
   - DNS 生效通常需要 1-24 小时
   - Vercel 会自动配置 HTTPS 证书

---

## 🎨 优化建议

### 1. 配置环境变量

如果你的 DApp 需要不同的配置（开发/生产），在 Vercel 中设置环境变量：

1. 进入项目设置 → Environment Variables
2. 添加变量：
   ```
   VITE_CONTRACT_ADDRESS=0x你的合约地址
   VITE_CHAIN_ID=97
   ```

3. 在代码中使用：
   ```javascript
   const CONTRACT_ADDRESS = import.meta.env.VITE_CONTRACT_ADDRESS
   ```

### 2. 添加 _headers 文件（Netlify）

在 `public` 文件夹创建 `_headers` 文件：

```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```

### 3. 添加 vercel.json 配置

在项目根目录创建 `vercel.json`：

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        }
      ]
    }
  ]
}
```

---

## 📱 分享你的 DApp

部署完成后，你可以通过以下方式分享：

1. **直接分享链接**
   ```
   https://tax-token-dapp.vercel.app
   ```

2. **生成二维码**
   - 使用工具：[qr-code-generator.com](https://www.qr-code-generator.com/)
   - 用户扫码即可访问

3. **在社交媒体宣传**
   - Twitter
   - Discord
   - Telegram
   - Reddit (r/CryptoMoonShots)

4. **添加到 DApp 列表**
   - [DappRadar](https://dappradar.com/)
   - [DeFi Pulse](https://defipulse.com/)
   - [CoinGecko](https://www.coingecko.com/)

---

## ❓ 常见问题

### Q1: 部署后钱包无法连接？

**A:** 确保：
1. 用户已安装 MetaMask
2. 用户切换到正确的网络（BSC Testnet/Mainnet）
3. 检查浏览器控制台是否有错误

### Q2: 页面刷新后显示 404？

**A:** 需要配置路由重定向：

**Vercel:** 创建 `vercel.json` (见上方配置)

**Netlify:** 在 `public` 文件夹创建 `_redirects` 文件：
```
/*    /index.html   200
```

### Q3: 合约地址需要修改吗？

**A:** 如果你的合约在不同网络：
1. 在 `constants.js` 中更新合约地址
2. 更新 `REQUIRED_CHAIN_ID`
3. 重新部署

### Q4: 如何查看访问量？

**A:**
- **Vercel:** 项目面板 → Analytics
- **Netlify:** 项目面板 → Analytics
- **Google Analytics:** 添加跟踪代码到 `index.html`

### Q5: 部署后样式丢失？

**A:** 检查：
1. `index.css` 是否正确导入
2. Tailwind 配置是否正确
3. 构建日志是否有错误

### Q6: 如何更新部署的版本？

**A:**
```bash
# 方式 1：自动部署（推荐）
git add .
git commit -m "更新内容"
git push
# Vercel/Netlify 会自动检测并部署

# 方式 2：手动部署（Vercel CLI）
vercel --prod
```

---

## 🔒 安全建议

1. **永远不要在代码中硬编码私钥**
2. **使用环境变量存储敏感信息**
3. **定期更新依赖包**
   ```bash
   npm audit
   npm update
   ```
4. **启用 HTTPS**（Vercel/Netlify 自动启用）
5. **添加安全头部**（见优化建议）

---

## 📊 监控和维护

### 1. 设置错误监控

推荐使用 [Sentry](https://sentry.io)：

```bash
npm install @sentry/react
```

### 2. 性能监控

- 使用 Vercel Analytics
- 或集成 Google Analytics

### 3. 定期备份

```bash
# 定期推送到 GitHub
git push origin main

# 导出构建文件
npm run build
# 备份 dist/ 文件夹
```

---

## 🎓 下一步

1. ✅ 部署成功
2. ✅ 测试所有功能
3. ✅ 分享给用户
4. 📈 收集反馈
5. 🔄 持续改进

---

## 📞 获取帮助

- **Vercel 文档**: [vercel.com/docs](https://vercel.com/docs)
- **Netlify 文档**: [docs.netlify.com](https://docs.netlify.com)
- **Vite 文档**: [vitejs.dev](https://vitejs.dev)

---

## 🎉 恭喜！

现在全世界的用户都可以访问你的 DApp 了！

记得在社交媒体上分享你的成果！🚀
